<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exercise.mapper.NotificationMapper">
    
    <resultMap id="BaseResultMap" type="com.exercise.entity.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="type" property="type"/>
        <result column="related_id" property="relatedId"/>
        <result column="is_read" property="isRead"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    
    <insert id="insert" parameterType="com.exercise.entity.Notification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (user_id, title, content, type, related_id, is_read)
        VALUES (#{userId}, #{title}, #{content}, #{type}, #{relatedId}, #{isRead})
    </insert>
    
    <update id="update" parameterType="com.exercise.entity.Notification">
        UPDATE notification
        SET title = #{title}, content = #{content}, is_read = #{isRead}
        WHERE id = #{id}
    </update>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM notification WHERE id = #{id}
    </select>
    
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM notification WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    
    <select id="selectUnreadByUserId" resultMap="BaseResultMap">
        SELECT * FROM notification WHERE user_id = #{userId} AND is_read = 0 ORDER BY create_time DESC
    </select>
    
    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM notification WHERE user_id = #{userId} AND is_read = 0
    </select>
    
    <update id="markAsRead">
        UPDATE notification SET is_read = 1 WHERE id = #{id}
    </update>
    
    <update id="markAllAsRead">
        UPDATE notification SET is_read = 1 WHERE user_id = #{userId}
    </update>
    
    <delete id="deleteById">
        DELETE FROM notification WHERE id = #{id}
    </delete>
</mapper>
