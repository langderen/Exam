<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exercise.mapper.BookPurchaseMapper">
    
    <resultMap id="BaseResultMap" type="com.exercise.entity.BookPurchase">
        <id column="id" property="id"/>
        <result column="book_id" property="bookId"/>
        <result column="user_id" property="userId"/>
        <result column="price" property="price"/>
        <result column="purchase_time" property="purchaseTime"/>
    </resultMap>
    
    <resultMap id="DetailResultMap" type="com.exercise.entity.BookPurchase" extends="BaseResultMap">
        <result column="book_name" property="bookName"/>
        <result column="book_desc" property="bookDesc"/>
        <result column="username" property="username"/>
    </resultMap>
    
    <select id="selectByBookAndUser" resultMap="BaseResultMap">
        SELECT * FROM book_purchase WHERE book_id = #{bookId} AND user_id = #{userId}
    </select>
    
    <select id="selectByUserId" resultMap="DetailResultMap">
        SELECT p.*, b.book_name, b.book_desc, u.username
        FROM book_purchase p
        LEFT JOIN exercise_book b ON p.book_id = b.id
        LEFT JOIN sys_user u ON p.user_id = u.id
        WHERE p.user_id = #{userId}
        ORDER BY p.purchase_time DESC
    </select>
    
    <select id="selectAll" resultMap="DetailResultMap">
        SELECT p.*, b.book_name, b.book_desc, u.username
        FROM book_purchase p
        LEFT JOIN exercise_book b ON p.book_id = b.id
        LEFT JOIN sys_user u ON p.user_id = u.id
        ORDER BY p.purchase_time DESC
    </select>
    
    <insert id="insert" parameterType="com.exercise.entity.BookPurchase" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO book_purchase (book_id, user_id, price, purchase_time)
        VALUES (#{bookId}, #{userId}, #{price}, #{purchaseTime})
    </insert>
    
    <delete id="deleteById">
        DELETE FROM book_purchase WHERE id = #{id}
    </delete>
</mapper>
