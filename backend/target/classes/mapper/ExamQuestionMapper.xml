<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exercise.mapper.ExamQuestionMapper">
    
    <resultMap id="BaseResultMap" type="com.exercise.entity.ExamQuestion">
        <id column="id" property="id"/>
        <result column="exam_id" property="examId"/>
        <result column="question_id" property="questionId"/>
        <result column="question_seq" property="questionSeq"/>
        <result column="user_answer" property="userAnswer"/>
        <result column="is_correct" property="isCorrect"/>
        <result column="is_answered" property="isAnswered"/>
        <result column="ai_score" property="aiScore"/>
        <result column="ai_comment" property="aiComment"/>
        <result column="create_time" property="createTime"/>
    </resultMap>
    
    <insert id="insert" parameterType="com.exercise.entity.ExamQuestion">
        INSERT INTO exam_question (exam_id, question_id, question_seq, user_answer, is_correct, is_answered, ai_score, ai_comment)
        VALUES (#{examId}, #{questionId}, #{questionSeq}, #{userAnswer}, #{isCorrect}, #{isAnswered}, #{aiScore}, #{aiComment})
    </insert>
    
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO exam_question (exam_id, question_id, question_seq, user_answer, is_correct, is_answered, ai_score, ai_comment)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.examId}, #{item.questionId}, #{item.questionSeq}, #{item.userAnswer}, #{item.isCorrect}, #{item.isAnswered}, #{item.aiScore}, #{item.aiComment})
        </foreach>
    </insert>
    
    <update id="update" parameterType="com.exercise.entity.ExamQuestion">
        UPDATE exam_question
        SET user_answer = #{userAnswer}, is_correct = #{isCorrect}, is_answered = #{isAnswered}, ai_score = #{aiScore}, ai_comment = #{aiComment}
        WHERE id = #{id}
    </update>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM exam_question WHERE id = #{id}
    </select>
    
    <select id="selectByExamId" resultMap="BaseResultMap">
        SELECT * FROM exam_question WHERE exam_id = #{examId} ORDER BY question_seq ASC
    </select>
    
    <update id="updateAnswer">
        UPDATE exam_question
        SET user_answer = #{userAnswer}, is_answered = #{isAnswered}
        WHERE exam_id = #{examId} AND question_id = #{questionId}
    </update>
    
    <update id="updateCorrect">
        UPDATE exam_question
        SET is_correct = #{isCorrect}
        WHERE exam_id = #{examId} AND question_id = #{questionId}
    </update>
    
    <update id="updateAiScore">
        UPDATE exam_question
        SET is_correct = #{isCorrect}, ai_score = #{aiScore}, ai_comment = #{aiComment}
        WHERE exam_id = #{examId} AND question_id = #{questionId}
    </update>
    
    <delete id="deleteByExamId">
        DELETE FROM exam_question WHERE exam_id = #{examId}
    </delete>
</mapper>
