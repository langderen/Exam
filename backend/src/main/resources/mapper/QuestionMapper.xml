<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exercise.mapper.QuestionMapper">
    
    <resultMap id="BaseResultMap" type="com.exercise.entity.Question">
        <id column="id" property="id"/>
        <result column="book_id" property="bookId"/>
        <result column="type_id" property="typeId"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="content_image" property="contentImage" jdbcType="VARCHAR"/>
        <result column="options" property="options" jdbcType="VARCHAR"/>
        <result column="options_image" property="optionsImage" jdbcType="VARCHAR"/>
        <result column="answer" property="answer" jdbcType="VARCHAR"/>
        <result column="analysis" property="analysis" jdbcType="VARCHAR"/>
        <result column="difficulty" property="difficulty"/>
        <result column="attachment" property="attachment" jdbcType="VARCHAR"/>
        <result column="attachment_name" property="attachmentName" jdbcType="VARCHAR"/>
        <result column="ai_scoring" property="aiScoring"/>
        <result column="key_points" property="keyPoints" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime"/>
        <result column="type_name" property="typeName"/>
        <result column="book_name" property="bookName"/>
        <result column="is_collected" property="isCollected"/>
        <result column="error_count" property="errorCount"/>
    </resultMap>
    
    <select id="selectByBookId" resultMap="BaseResultMap">
        SELECT q.*, qt.type_name, eb.book_name
        FROM question q
        LEFT JOIN question_type qt ON q.type_id = qt.id
        LEFT JOIN exercise_book eb ON q.book_id = eb.id
        WHERE q.book_id = #{bookId}
        ORDER BY q.id ASC
    </select>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT q.*, qt.type_name, eb.book_name
        FROM question q
        LEFT JOIN question_type qt ON q.type_id = qt.id
        LEFT JOIN exercise_book eb ON q.book_id = eb.id
        WHERE q.id = #{id}
    </select>
    
    <select id="selectBySeq" resultMap="BaseResultMap">
        SELECT q.*, qt.type_name, eb.book_name
        FROM question q
        LEFT JOIN question_type qt ON q.type_id = qt.id
        LEFT JOIN exercise_book eb ON q.book_id = eb.id
        WHERE q.book_id = #{bookId}
        ORDER BY q.id ASC
        LIMIT #{offset}, 1
    </select>
    
    <select id="selectSequence" resultMap="BaseResultMap">
        SELECT q.*, qt.type_name, eb.book_name
        FROM question q
        LEFT JOIN question_type qt ON q.type_id = qt.id
        LEFT JOIN exercise_book eb ON q.book_id = eb.id
        WHERE q.book_id = #{bookId}
        ORDER BY q.id ASC
        LIMIT #{offset}, 1
    </select>
    
    <select id="selectRandom" resultMap="BaseResultMap">
        SELECT q.*, qt.type_name, eb.book_name
        FROM question q
        LEFT JOIN question_type qt ON q.type_id = qt.id
        LEFT JOIN exercise_book eb ON q.book_id = eb.id
        WHERE q.book_id = #{bookId}
        AND q.id NOT IN (
            SELECT question_id FROM question_record WHERE user_id = #{userId}
        )
        ORDER BY RAND()
        LIMIT 1
    </select>
    
    <select id="selectRandomByTypes" resultMap="BaseResultMap">
        SELECT q.*, qt.type_name, eb.book_name
        FROM question q
        LEFT JOIN question_type qt ON q.type_id = qt.id
        LEFT JOIN exercise_book eb ON q.book_id = eb.id
        WHERE q.book_id = #{bookId}
        <if test="typeIds != null and typeIds.size() > 0">
            AND q.type_id IN
            <foreach collection="typeIds" item="typeId" open="(" separator="," close=")">
                #{typeId}
            </foreach>
        </if>
        ORDER BY RAND()
        LIMIT #{count}
    </select>
    
    <select id="countByBookId" resultType="int">
        SELECT COUNT(*) FROM question WHERE book_id = #{bookId}
    </select>
    
    <insert id="insert" parameterType="com.exercise.entity.Question" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO question (book_id, type_id, content, content_image, options, options_image, answer, analysis, difficulty, attachment, attachment_name, ai_scoring, key_points)
        VALUES (#{bookId}, #{typeId}, #{content}, #{contentImage}, #{options}, #{optionsImage}, #{answer}, #{analysis}, #{difficulty}, #{attachment}, #{attachmentName}, #{aiScoring}, #{keyPoints})
    </insert>
    
    <update id="update" parameterType="com.exercise.entity.Question">
        UPDATE question
        <set>
            <if test="typeId != null">type_id = #{typeId},</if>
            <if test="content != null">content = #{content},</if>
            <if test="contentImage != null">content_image = #{contentImage},</if>
            <if test="options != null">options = #{options},</if>
            <if test="optionsImage != null">options_image = #{optionsImage},</if>
            <if test="answer != null">answer = #{answer},</if>
            <if test="analysis != null">analysis = #{analysis},</if>
            <if test="difficulty != null">difficulty = #{difficulty},</if>
            <if test="attachment != null">attachment = #{attachment},</if>
            <if test="attachmentName != null">attachment_name = #{attachmentName},</if>
            <if test="aiScoring != null">ai_scoring = #{aiScoring},</if>
            <if test="keyPoints != null">key_points = #{keyPoints},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById">
        DELETE FROM question WHERE id = #{id}
    </delete>
    
    <select id="selectListByBookId" resultMap="BaseResultMap">
        SELECT q.*, qt.type_name, eb.book_name
        FROM question q
        LEFT JOIN question_type qt ON q.type_id = qt.id
        LEFT JOIN exercise_book eb ON q.book_id = eb.id
        WHERE q.book_id = #{bookId}
        ORDER BY q.id ASC
    </select>
</mapper>
